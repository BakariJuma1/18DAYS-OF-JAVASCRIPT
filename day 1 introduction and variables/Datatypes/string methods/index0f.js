//indexOf() takes a substring and if it exists in aa string it returns the first position of the substring
//if it does not exist it returns -1

let string = "30 days of javasript";

string.indexOf(substring);
console.log(string.indexOf("days"));
